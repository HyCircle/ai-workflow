# .workflow/kit/workflow.env.example — 工作流单机/项目级配置的**模板**(随 kit copy,在 exclude 内)。
# 用法:装机后 `cp .workflow/kit/workflow.env.example .workflow/workflow.env`,按本机技术栈/模型填。
#   实机的 .workflow/workflow.env 被 gitignore(每机不同,不进版本库);只本模板在 kit 快照里。
# 换项目 / 换模型只动 workflow.env 这一处;scripts source 它取默认值,skill 命令示例以它为准。
# 见 AGENTS.md「项目工具约定」。

# —— 命令(按项目技术栈填) ——
WF_TEST_CMD="uv run pytest"          # 跑测试
WF_PY="uv run python"                # 跑仓内 python 工具(check_docs 等)

# —— 派单模型分档(便宜劳力;「后端/模型」,后端 cursor|codex,省前缀=cursor) ——
# 换型号/换厂只改这里;run_worker.sh 读这三个做默认值,别在脚本/skill 里写死型号。
# 独立审查默认选择与 worker 不同的模型家族;负责人仍需判断证据和整体方案。
#   注意「cursor/」「codex/」是**派单 CLI 后端**,不等于模型家族——下例三档正好三家互异:
#   worker=composer(Cursor 家) · 默认验收=grok(Grok 家) · 强审/双验收=gpt-terra(GPT 家,Codex 官方)。
WF_WORKER_MODEL="cursor/composer-2.5"              # 默认执行(量大最省,用标准档非 -fast)
WF_REVIEW_MODEL="cursor/cursor-grok-4.6-high"      # 独立审查默认档(Grok 家)
WF_REVIEW_MODEL_STRONG="codex/gpt-5.6-terra"       # 更强判断 / 有明确额外收益的第二审(GPT 家)
