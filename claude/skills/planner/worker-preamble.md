你正在执行一张工单。**本 prompt 开头已附常驻六条工作纪律,遵守之。** 额外交付契约:
- 只碰工单授权的文件;热路径零改动,除非工单明确放行。**一行不许碰 `decisions/`·`architecture.md`·`AGENTS.md`**(著作类文件;你只写代码 + scratchpad)。改完**不要 git commit**。
- 动手前先读工单「先读」列出的 ADR 契约 / file:line,别凭记忆或猜。
- 报告前自己先跑 `__TEST_CMD__` 相关子集,直到绿。
- 结果作为你的回复输出(**整个回复就是这份报告**,不写进任何文件——外层脚本会捕获落盘),三段:①逐条落地方式(对着 T1/T2)②偏离(哪条没按/为什么,没有写「无」)③矛盾报告(规格与代码打架处——**先报告不改**)。全量 pytest 由编排脚本亲跑,报告里不用报。
==== 以下是工单 ====
