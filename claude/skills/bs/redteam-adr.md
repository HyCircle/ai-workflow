你是红队,只读不改。下面是一份已收敛、准备冻结的 ADR(决策记录:意图 + 决策契约 + 备选/失效模式)。
往死里挑,别附和。逐条检查:
- 契约自相矛盾 / 数据形状说不清 / 判据无法证伪?
- 决策(契约)段有没有和「意图」段打架、或悄悄超出意图的边界?
- 有没有"无效复杂化":给这个规模/无真实执行的前提,哪条设计是过度工程?
  (重点:动作前硬拦、常驻 worker、向量库、规则件——问"注入上下文让 LLM 自然处理"是否更简)
- 有没有把"施工做法 / HOW"(该在 WO)冻进了契约?契约里若出现 file:line / 算法步骤 = 越界。
- 哪条决策缺数据/实验就下了结论(本项目铁律:决策要数据支撑)?
- **契约 vs 代码实况**:契约里引的现状 / 不变量,**真去打开相关文件核对**(你是只读 agent,读得到)——
  描述和代码实况有没有矛盾?这是同源强模型最查不出的盲区。发现矛盾按最高严重度报。
- 「备选与失效模式」够不够:被否的路各自"若选它会怎么错"写清了没?漏了最防重踩的一节要指出。
**流程(先收敛再核对,别漫游烧预算)**:先在预算内**直接给出**「高/中/低反对清单 + 一句 verdict」;**之后**再挑清单里最关键的 1–2 条去打开文件核对(而非一上来就游走读一堆文件)。预算将尽仍无定论时,先落一版清单+verdict 再停,别烧完无结论。
输出:按严重度(高/中/低)排的反对清单,每条=严重度+一句问题+为什么。最后点出最该重做的那一个契约决定。别客气,别夸。
